/* Pricing Page Styles */
.pcg-container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 1rem;
}

.pcg-header {
  text-align: center;
  margin-bottom: 2.5rem;
}

.pcg-title {
  font-size: 2rem;
  font-weight: 700;
  margin-bottom: 0.5rem;
  color: var(--text);
}

.pcg-subtitle {
  font-size: 1.1rem;
  color: var(--text-secondary);
  max-width: 600px;
  margin: 0 auto;
}

.pcg-cards-container {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 2rem;
  margin-bottom: 3rem;
}

.pcg-card {
  background-color: var(--card-bg);
  border-radius: 12px;
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.08);
  padding: 2rem;
  position: relative;
  transition: transform 0.3s ease, box-shadow 0.3s ease;
  display: flex;
  flex-direction: column;
  border: 1px solid var(--border);
}

.pcg-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 12px 30px rgba(0, 0, 0, 0.12);
}

.pcg-featured {
  border: 2px solid var(--primary);
  transform: scale(1.02);
}

.pcg-featured:hover {
  transform: translateY(-5px) scale(1.02);
}

.pcg-popular-tag {
  position: absolute;
  top: -12px;
  right: 20px;
  background-color: var(--primary);
  color: var(--text-primary);
  padding: 0.3rem 1rem;
  border-radius: 20px;
  font-size: 0.85rem;
  font-weight: 600;
}

.pcg-card-header {
  margin-bottom: 1.5rem;
}

.pcg-plan-name {
  font-size: 1.5rem;
  font-weight: 700;
  margin-bottom: 0.3rem;
  color: var(--text);
}

.pcg-plan-duration {
  font-size: 0.9rem;
  color: var(--text-secondary);
}

.pcg-price {
  display: flex;
  align-items: baseline;
  margin-bottom: 0.5rem;
}

.pcg-currency {
  font-size: 1.5rem;
  font-weight: 600;
  margin-right: 0.2rem;
  color: var(--text);
}

.pcg-amount {
  font-size: 3.5rem;
  font-weight: 700;
  line-height: 1;
  color: var(--text);
}

.pcg-period {
  font-size: 1rem;
  color: var(--text-secondary);
  margin-left: 0.3rem;
}

.pcg-savings {
  background-color: rgba(16, 185, 129, 0.1);
  color: var(--success);
  padding: 0.3rem 0.8rem;
  border-radius: 20px;
  font-size: 0.85rem;
  font-weight: 600;
  display: inline-block;
  margin-bottom: 1.5rem;
}

.pcg-features {
  margin: 1.5rem 0;
  flex-grow: 1;
}

.pcg-feature {
  display: flex;
  align-items: center;
  margin-bottom: 1rem;
  color: var(--text);
}

.pcg-feature i {
  color: var(--success);
  margin-right: 0.8rem;
  font-size: 1.1rem;
}

.pcg-button-container {
  margin-top: 1rem;
}

.pcg-subscribe-btn {
  width: 100%;
  padding: 0.9rem;
  border-radius: 8px;
  background-color: var(--primary);
  color: var(--text-primary);
  font-weight: 600;
  border: none;
  cursor: pointer;
  transition: background-color 0.2s ease;
  font-size: 1rem;
}

.pcg-subscribe-btn:hover {
  background-color: var(--primary-hover);
}

.pcg-subscribe-btn:disabled {
  opacity: 0.6;
  cursor: not-allowed;
}

/* Spinner Button */
.pcg-subscribe-btn .fa-spin {
  margin-right: 0.5rem;
}

/* Benefits Section */
.pcg-benefits {
  margin: 4rem 0;
}

.pcg-benefits-title {
  text-align: center;
  font-size: 1.8rem;
  font-weight: 700;
  margin-bottom: 2rem;
  color: var(--text);
}

.pcg-benefits-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 2rem;
}

.pcg-benefit-item {
  text-align: center;
  padding: 1.5rem;
  border-radius: 12px;
  background-color: var(--card-bg);
  border: 1px solid var(--border);
  transition: transform 0.3s ease;
}

.pcg-benefit-item:hover {
  transform: translateY(-5px);
}

.pcg-benefit-icon {
  width: 60px;
  height: 60px;
  border-radius: 50%;
  background-color: rgba(0, 0, 0, 0.05);
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto 1rem;
}

[data-theme="dark"] .pcg-benefit-icon {
  background-color: rgba(255, 255, 255, 0.1);
}

.pcg-benefit-icon i {
  font-size: 1.5rem;
  color: var(--primary);
}

.pcg-benefit-item h4 {
  font-size: 1.2rem;
  font-weight: 600;
  margin-bottom: 0.8rem;
  color: var(--text);
}

.pcg-benefit-item p {
  color: var(--text-secondary);
  font-size: 0.95rem;
  line-height: 1.5;
}

/* FAQ Section */
.pcg-faq {
  margin: 4rem 0 2rem;
}

.pcg-faq-title {
  text-align: center;
  font-size: 1.8rem;
  font-weight: 700;
  margin-bottom: 2rem;
  color: var(--text);
}

.pcg-faq-item {
  margin-bottom: 1rem;
  border: 1px solid var(--border);
  border-radius: 8px;
  overflow: hidden;
}

.pcg-faq-question {
  padding: 1.2rem;
  background-color: var(--card-bg);
  display: flex;
  justify-content: space-between;
  align-items: center;
  cursor: pointer;
  font-weight: 600;
  color: var(--text);
}

.pcg-faq-question i {
  transition: transform 0.3s ease;
}

.pcg-faq-question-open i {
  transform: rotate(180deg);
}

.pcg-faq-answer {
  max-height: 0;
  overflow: hidden;
  transition: max-height 0.3s ease, padding 0.3s ease;
  background-color: var(--card-bg);
  color: var(--text-secondary);
  line-height: 1.6;
}

.pcg-faq-answer-open {
  max-height: 200px;
  padding: 1rem 1.2rem 1.5rem;
}

/* Skeleton Loading Animation */
.skeleton-card {
  min-height: 400px;
}

.skeleton-pulse {
  background: linear-gradient(90deg, var(--card-bg) 0%, var(--border) 50%, var(--card-bg) 100%);
  background-size: 200% 100%;
  animation: pulse 1.5s ease-in-out infinite;
}

@keyframes pulse {
  0% {
    background-position: 100% 0;
  }
  100% {
    background-position: -100% 0;
  }
}

/* Responsive Adjustments */
@media (max-width: 768px) {
  .pcg-title {
    font-size: 1.8rem;
  }

  .pcg-subtitle {
    font-size: 1rem;
  }

  .pcg-cards-container {
    gap: 1.5rem;
  }

  .pcg-featured {
    transform: scale(1);
  }

  .pcg-featured:hover {
    transform: translateY(-5px) scale(1);
  }

  .pcg-benefits-grid,
  .pcg-testimonials-container {
    gap: 1.5rem;
  }
}

@media (max-width: 480px) {
  .pcg-container {
    padding: 0.5rem;
  }

  .pcg-card {
    padding: 1.5rem;
  }

  .pcg-amount {
    font-size: 3rem;
  }
}


