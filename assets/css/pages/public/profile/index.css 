/* Profile Page Styles */

/* Container for the entire profile page */
.pfp-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 2rem 1rem;
  max-width: 600px;
  margin: 0 auto;
}

/* Avatar section */
.pfp-avatar-section {
  margin-bottom: 2rem;
  position: relative;
}

.pfp-avatar {
  width: 120px;
  height: 120px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 3rem;
  font-weight: bold;
  color: var(--text-primary);
  background: linear-gradient(135deg, var(--primary), var(--primary-hover));
  box-shadow: 0 4px 20px var(--shadow);
  position: relative;
  overflow: hidden;
  border: 4px solid var(--card-bg);
}

/* Info section */
.pfp-info-section {
  width: 100%;
  text-align: center;
  margin-bottom: 2rem;
}

.pfp-username {
  font-size: 1.8rem;
  font-weight: bold;
  margin: 0 0 0.5rem 0;
  color: var(--text);
}

.pfp-email {
  font-size: 1rem;
  color: var(--text-secondary);
  margin: 0 0 1.5rem 0;
}

/* Details section */
.pfp-details {
  background-color: var(--card-bg);
  border-radius: 12px;
  padding: 1.5rem;
  width: 100%;
  box-shadow: 0 2px 10px var(--shadow);
  margin-top: 1rem;
}

.pfp-detail-item {
  display: flex;
  justify-content: space-between;
  padding: 0.75rem 0;
  border-bottom: 1px solid var(--divider);
}

.pfp-detail-item:last-child {
  border-bottom: none;
}

.pfp-detail-label {
  font-weight: 500;
  color: var(--text-secondary);
}

.pfp-detail-value {
  font-weight: 600;
  color: var(--text);
}

/* Status indicators */
.pfp-status-active {
  color: var(--success);
  position: relative;
  padding-left: 18px;
}

.pfp-status-active::before {
  content: "";
  position: absolute;
  left: 0;
  top: 50%;
  transform: translateY(-50%);
  width: 10px;
  height: 10px;
  border-radius: 50%;
  background-color: var(--success);
}

.pfp-status-inactive {
  color: var(--warning);
  position: relative;
  padding-left: 18px;
}

.pfp-status-inactive::before {
  content: "";
  position: absolute;
  left: 0;
  top: 50%;
  transform: translateY(-50%);
  width: 10px;
  height: 10px;
  border-radius: 50%;
  background-color: var(--warning);
}

/* Action buttons */
.pfp-actions {
  width: 100%;
  display: flex;
  justify-content: center;
  margin-top: 1rem;
}

.pfp-logout-btn {
  padding: 0.75rem 2rem;
  border-radius: 8px;
  background-color: var(--danger);
  color: white;
  border: none;
  font-weight: 600;
  display: flex;
  align-items: center;
  gap: 0.5rem;
  cursor: pointer;
  transition: all 0.2s ease;
}

.pfp-logout-btn:hover {
  background-color: var(--danger);
  opacity: 0.9;
  transform: translateY(-2px);
}

.pfp-logout-btn i {
  font-size: 1rem;
}

/* Skeleton loading styles */
.pfp-skeleton-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 2rem 1rem;
  max-width: 600px;
  margin: 0 auto;
}

.pfp-avatar-skeleton {
  width: 120px;
  height: 120px;
  border-radius: 50%;
  background-color: var(--card-bg);
  margin-bottom: 2rem;
}

.pfp-info-skeleton {
  width: 100%;
  text-align: center;
}

.pfp-name-skeleton {
  height: 2rem;
  width: 60%;
  margin: 0 auto 1rem auto;
  border-radius: 4px;
  background-color: var(--card-bg);
}

.pfp-email-skeleton {
  height: 1rem;
  width: 80%;
  margin: 0 auto 2rem auto;
  border-radius: 4px;
  background-color: var(--card-bg);
}

.pfp-details-skeleton {
  background-color: var(--card-bg);
  border-radius: 12px;
  padding: 1.5rem;
  width: 100%;
  box-shadow: 0 2px 10px var(--shadow);
}

.pfp-detail-item-skeleton {
  height: 1.5rem;
  width: 100%;
  margin-bottom: 1rem;
  border-radius: 4px;
  background-color: var(--divider);
}

/* Error state */
.pfp-error-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 3rem 1rem;
}

.pfp-error-container i {
  font-size: 3rem;
  color: var(--danger);
  margin-bottom: 1rem;
}

.pfp-error-container h3 {
  font-size: 1.5rem;
  margin-bottom: 0.5rem;
  color: var(--text);
}

.pfp-error-container p {
  color: var(--text-secondary);
  margin-bottom: 1.5rem;
}

/* Animation */
.pulse {
  animation: pulse 1.5s infinite;
}

@keyframes pulse {
  0% {
    opacity: 0.6;
  }
  50% {
    opacity: 0.3;
  }
  100% {
    opacity: 0.6;
  }
}

/* Responsive adjustments */
@media (min-width: 768px) {
  .pfp-container {
    padding: 3rem 2rem;
  }

  .pfp-avatar {
    width: 150px;
    height: 150px;
    font-size: 4rem;
  }
}

/* Dark mode specific adjustments */
[data-theme="dark"] .pfp-avatar {
  border-color: var(--card-bg);
}

[data-theme="dark"] .pfp-details {
  background-color: var(--card-bg);
}

